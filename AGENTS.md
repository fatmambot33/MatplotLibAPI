# AGENTS

## Commit Checklist

- Run `pydocstyle MatplotLibAPI`.
- Run `pyright MatplotLibAPI`.
- Run `pytest -q`.

## Style Notes
- Prefer numpy-style docstrings with explicit `Args` and `Returns` sections.
- Keep code pythonic and maintainable.
